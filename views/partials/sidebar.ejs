<nav class="col-md-2 d-none d-lg-block bg-light sidebar">
  <div class="sidebar-sticky">
    <ul class="nav flex-column">
      <li class="nav-item">
        <a class="nav-link pt-4 pb-4 <% if (title == 'Control Dashboard') {%> active <%} %>" href="/control"><i class="fas fa-cog" style="font-size:1.5em;padding-right:5px"></i>Control Dashboard <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">

        <a class="nav-link  <% if (title == 'Historic Dashboard') {%> active <%} %>" href="/historic"><i class="fas fa-history" style="font-size:1.5em;padding-right:5px"></i>Historic Dashboard<span class="sr-only">(current)</span></a>
      </li>
      <div>
        <hr>
        <% if (title == 'Control Dashboard') {%>
          <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
            <span>Instruments</span>
            <a class="d-flex align-items-center text-muted" href="#">
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-plus-circle"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="8" x2="12" y2="16"></line><line x1="8" y1="12" x2="16" y2="12"></line></svg>
            </a>
          </h6>
          <ul class="nav flex-column mb-2">
            <% instruments.forEach(function(instrument) { %>
              <li class="nav-item">
                <a class="nav-link" href="control/<%= instrument.deviceId %>"><i class="fas fa-cube" style="font-size:1.5em;padding-right:5px"></i><%= instrument.deviceId %></a>
              </li>
            <% }); %>
          </ul>
          <%} %>
          <% if (title == 'Show Instrument' || title == 'Schedule Instrument') {%>
            <%- include ../control/panel.ejs %>
          <%} %>
      </div>
  </div>
</nav>
